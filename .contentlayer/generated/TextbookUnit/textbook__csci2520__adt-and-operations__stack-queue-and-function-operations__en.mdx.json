{
  "title": "1.1 ADT operations: stack, queue, and function pointers",
  "description": "Study ADT contracts first, then follow stack and queue operations through concrete C implementations and function-pointer-based dispatch.",
  "body": {
    "raw": "\n## Why this section starts with contracts\n\nIn CSCI2520, the first mistake is usually not a missing semicolon. It is a\nmissing contract.\n\nAn abstract data type tells us four things before we commit to a particular\nrepresentation:\n\n- what operations are legal,\n- what each operation guarantees,\n- what the client may assume after a call,\n- how empty-state and error cases are handled.\n\nThat separation matters because the same stack or queue interface can be backed\nby a fixed array, a dynamic array, or a linked list. The client code should not\nneed to change when the representation changes.\n\n<Definition title=\"ADT contract versus implementation\">\nAn ADT contract names the operations and their meaning.\nAn implementation chooses the data fields, memory layout, and update strategy.\nThe contract is the promise; the representation is the mechanism.\n</Definition>\n\n## A stack is a disciplined top-only structure\n\nThe lecture slides define a stack as a pile that can be accessed only from the\ntop. That is not just an analogy. It is the invariant that makes stack code\npredictable.\n\n<Definition title=\"Stack core operations\">\nFor a stack `S`:\n\n- `push(x)`: place `x` on the top of `S`,\n- `pop()`: remove and return the top element,\n- `top()`: inspect the top element without removing it,\n- `isEmpty()`: test whether the stack has no elements,\n- `StackDepth()`: return the number of stored elements.\n</Definition>\n\nThe key law is LIFO, last in first out. If `a` is pushed before `b`, then `b`\nmust come out first unless an error interrupts the process.\n\n<WorkedExample title=\"Tracing a stack by state\">\nStart with an empty stack.\n\n1. `push(10)` gives `[10]`\n2. `push(20)` gives `[10, 20]`\n3. `top()` returns `20` and leaves the state unchanged\n4. `pop()` returns `20` and the stack becomes `[10]`\n</WorkedExample>\n\n## The stack interface hides the representation\n\nThe lecture makes a point of the opaque type pattern:\n\n```c\ntypedef struct stackCDT *stackADT;\ntypedef int stackElementT;\n\nstackADT EmptyStack(void);\nvoid Push(stackADT stack, stackElementT element);\nstackElementT Pop(stackADT stack);\nint StackDepth(stackADT stack);\nint StackIsEmpty(stackADT stack);\n```\n\nThis is not cosmetic. `stackADT` is a pointer to an incomplete structure, so the\nclient can hold and pass stacks around but cannot reach inside the structure.\nThat means the implementation can change from:\n\n- a fixed array of 100 entries,\n- to a dynamic array with `realloc`,\n- to another internal layout later,\n\nwithout rewriting client code.\n\n<TheoremCard title=\"Representation independence\">\nIf the client can use an ADT only through its declared operations, then the\nimplementation may change its internal representation without changing the\nclient-visible behavior.\n</TheoremCard>\n\n## How the stack is implemented in practice\n\nThe source slides show three important implementation ideas.\n\nFirst, a fixed array implementation stores values bottom to top and keeps a\n`count` field for the current depth:\n\n```c\nstruct stackCDT {\n   stackElementT elements[100];\n   int count;\n};\n```\n\nThis version is simple, but it has a hard capacity limit. If the depth grows\nbeyond 100, the implementation hits stack overflow at the representation level,\nnot the mathematical stack operation.\n\nSecond, a dynamic-array version stores an expandable pointer and a `size`\nfield:\n\n```c\nstruct stackCDT {\n   stackElementT *elements;\n   int count;\n   int size;\n};\n```\n\nWhen the array fills, `Push()` expands the storage with `realloc()`. That gives\nthe client a larger logical stack without changing the interface.\n\nThird, the slides also refine `Pop()` to recover memory more carefully when the\nunderlying array is no longer fully used. That is a reminder that a correct\ninterface is not enough; memory policy still matters.\n\n<WorkedExample title=\"A push/pop implementation sketch\">\n```c\nvoid Push(stackADT stack, stackElementT element) {\n   if (stack->count == stack->size) {\n      stack->size += 10;\n      stack->elements = realloc(\n         stack->elements,\n         stack->size * sizeof(stackElementT)\n      );\n   }\n   stack->elements[stack->count] = element;\n   stack->count++;\n}\n\nstackElementT Pop(stackADT stack) {\n   stack->count--;\n   return stack->elements[stack->count];\n}\n```\n\nThe code is intentionally direct:\n\n- `count` tracks the next free slot,\n- `push` stores at that slot and increments the count,\n- `pop` decrements the count and returns the old top value.\n</WorkedExample>\n\n## Queue semantics are different, even if the code looks similar\n\nThe queue ADT is the other side of the same design problem. The operations are\nsimilar in style, but the invariant is FIFO, first in first out.\n\n<Definition title=\"Queue core operations\">\nFor a queue `Q`:\n\n- `enqueue(x)`: add `x` at the tail,\n- `dequeue()`: remove and return the head element,\n- `front()`: inspect the head element without removing it,\n- `isEmpty()`: test whether the queue has no elements,\n- `QueueLength()`: return the number of stored elements.\n</Definition>\n\nThe lecture notes present the queue header in the same opaque-style pattern:\n\n```c\ntypedef struct queueCDT *queueADT;\ntypedef int queueElementT;\n\nqueueADT EmptyQueue(void);\nvoid Enqueue(queueADT queue, queueElementT element);\nqueueElementT Dequeue(queueADT queue);\nint QueueLength(queueADT queue);\nint QueueIsEmpty(queueADT queue);\n```\n\nAgain, the interface says what the user may do; it does not say how the queue is\nstored.\n\n## Queue implementations: array, circular array, linked list\n\nThe tutorial slides make the implementation tradeoffs explicit.\n\n- A fixed-size array is the simplest first version, but it wastes space when\n  elements are dequeued from the front and new values are appended at the tail.\n- A circular array fixes that waste by wrapping `front` and `rear` around with\n  the modulus operator.\n- A linked-list queue stores `head` and `tail` pointers and grows as long as the\n  heap can provide memory.\n\nThe linked-list version is especially useful when we want the queue to keep\naccepting work items without shifting existing elements around.\n\n```c\nstruct cellT {\n   queueElementT element;\n   struct cellT *next;\n};\n\nstruct queueCDT {\n   struct cellT *head;\n   struct cellT *tail;\n};\n```\n\nThat representation makes `enqueue` and `dequeue` conceptually simple:\n\n- a new node is linked at the tail,\n- the head node is removed and freed,\n- `QueueIsEmpty()` is just `head == NULL`.\n\n<WorkedExample title=\"Why a circular array is better than a plain array\">\nSuppose a queue alternates `enqueue` and `dequeue` many times.\n\n- In a plain array, values near the front can leave empty space that is not\n  reused efficiently.\n- In a circular array, the front and rear indices wrap around so the earlier\n  slots can be reused.\n\nThat is why the tutorial explicitly asks what happens if we `enqueue` and\n`dequeue` repeatedly for millions of operations.\n</WorkedExample>\n\n## Function pointers let C simulate method dispatch and callbacks\n\nThe lecture on function pointers is not random extra material. It explains how\nC can defer behavior until runtime while still staying type-safe.\n\nAt a minimum, a function pointer stores the address of code:\n\n```c\nint (*fp)(int);\n```\n\nThe pointer may point to any function with the matching prototype. That means\nthe type signature is the contract.\n\n<Definition title=\"Function-pointer rule\">\nA function pointer may point only to functions whose parameter list and return\ntype match the declared function-pointer type.\n</Definition>\n\nThe slides then move to a practical ADT example: extending a hash table with a\ncallback-based traversal operation.\n\n```c\ntypedef void (*hashtableFnT)(char *, void *);\n\nvoid ForEachEntryDo(hashtableFnT fp, hashtableADT table);\n```\n\nClient code can pass a callback:\n\n```c\nvoid DisplayWordCount(hashtableADT table) {\n   ForEachEntryDo(PrintEntry, table);\n}\n```\n\nThis pattern matters for ADTs because it lets the implementation perform the\niteration while the client controls what happens to each entry. The traversal\nlogic stays in one place, but the action can vary.\n\nThe same idea also appears in dispatch tables: a set of function pointers can\nselect the correct operation for a specific structure or command without a long\nchain of `if` statements.\n\n```c\nvoid ApplyOperator(char c, stackADT stack) {\n   int y = Pop(stack);\n   int x = Pop(stack);\n\n   switch (c) {\n   case '+': Push(stack, x + y); break;\n   case '-': Push(stack, x - y); break;\n   case '*': Push(stack, x * y); break;\n   case '/': Push(stack, x / y); break;\n   }\n}\n```\n\nIn this example, the stack stores operands while the operation table decides how\nto combine them. The code is small, but the concept is broad: function pointers\nturn C into a manually controlled dispatch system.\n\n<InteractiveWidget id=\"adt-stack-queue-stepper\" />\n\n## Testing an ADT contract with code\n\nOnce the interface is fixed, the next question is not “does it compile?” but\n“does every observable state transition still satisfy the contract?”\n\nOne practical method is to write tiny trace-based tests:\n\n```c\nstackADT s = EmptyStack();\nPush(s, 10);\nPush(s, 20);\nassert(StackDepth(s) == 2);\nassert(Pop(s) == 20);\nassert(Pop(s) == 10);\nassert(StackIsEmpty(s));\n```\n\nThis kind of test is valuable because it checks the contract at the ADT level,\nnot the memory layout level. If you later replace the array-backed stack with a\nlinked representation, the same test should still pass unchanged.\n\nThe same idea applies to queues: a short `enqueue`, `enqueue`, `dequeue`,\n`dequeue` trace should confirm that the first inserted element is still the\nfirst one removed, even after the representation changes.\n\n## Common mistakes\n\n<CommonMistake title=\"Mixing the stack and queue invariants\">\n`push`/`pop` belong to a stack; `enqueue`/`dequeue` belong to a queue. Renaming\nthe functions without preserving the access order breaks the ADT contract even\nif the program still compiles.\n</CommonMistake>\n\n<CommonMistake title=\"Forgetting error policy on empty structures\">\nThe slides explicitly treat empty-stack underflow and queue-empty cases as\nerrors that must be handled. Decide whether the function returns a status code,\na sentinel, or an error indicator before you code the interface.\n</CommonMistake>\n\n<CommonMistake title=\"Confusing interface stability with implementation stability\">\nThe interface should stay stable. The internal representation is what should be\nfree to change when the data structure evolves.\n</CommonMistake>\n\n## Quick checks\n\n<QuickCheck prompt=\"After `push(1)`, `push(2)`, `pop()`, what remains in the stack?\">\nApply LIFO strictly.\n</QuickCheck>\n\n<RevealSolution title=\"Answer\">\nOnly `1` remains.\n</RevealSolution>\n\n<QuickCheck prompt=\"After `enqueue(4)`, `enqueue(7)`, `dequeue()`, which element is returned?\">\nApply FIFO strictly.\n</QuickCheck>\n\n<RevealSolution title=\"Answer\">\n`4` is returned.\n</RevealSolution>\n\n<QuickCheck prompt=\"Why does `ForEachEntryDo(PrintEntry, table)` need a callback type, not just a generic pointer?\">\nFocus on type checking and matching parameter lists.\n</QuickCheck>\n\n<RevealSolution title=\"Guided solution\">\nThe callback type says exactly which function signatures are legal. A generic pointer does not preserve the parameter and return-type information, so the compiler cannot enforce the contract. The callback type keeps the traversal safe and explicit.\n</RevealSolution>\n\n## Exercises\n\n<QuickCheck prompt=\"Write a precondition and a postcondition for `Pop(stack)` on a non-empty stack.\">\nKeep the contract precise.\n</QuickCheck>\n\n<RevealSolution title=\"Guided solution\">\nPrecondition: the stack is not empty. Postcondition: the old top element is returned, the stack depth decreases by one, and the order of the remaining elements is unchanged.\n</RevealSolution>\n\n<QuickCheck prompt=\"Explain why a queue implemented with a linked list can support `enqueue` without shifting existing elements.\">\nTie your answer to `head` and `tail`.\n</QuickCheck>\n\n<RevealSolution title=\"Guided solution\">\nThe new node is linked after the current tail, and the tail pointer is updated to the new node. No earlier node has to move, so the existing queue order stays intact.\n</RevealSolution>\n\n<QuickCheck prompt=\"Write one short paragraph explaining how function pointers help testing an ADT implementation.\">\nUse the interface-versus-implementation distinction.\n</QuickCheck>\n\n<RevealSolution title=\"Guided solution\">\nFunction pointers let the same client code call the same signatures while the backend changes. A test suite can therefore exercise multiple implementations of the same ADT contract and catch violations without rewriting the tests.\n</RevealSolution>\n\n## Takeaway\n\nThe main idea is not “stack and queue are two data structures.” The real idea is\nthat ADT contracts define behavior, implementations choose storage, and function\npointers let C separate those responsibilities without losing control over the\ntypes.\n",
    "code": "var Component=(()=>{var k=Object.create;var h=Object.defineProperty;var g=Object.getOwnPropertyDescriptor;var T=Object.getOwnPropertyNames;var w=Object.getPrototypeOf,b=Object.prototype.hasOwnProperty;var v=(t,n)=>()=>(n||t((n={exports:{}}).exports,n),n.exports),q=(t,n)=>{for(var i in n)h(t,i,{get:n[i],enumerable:!0})},u=(t,n,i,r)=>{if(n&&typeof n==\"object\"||typeof n==\"function\")for(let a of T(n))!b.call(t,a)&&a!==i&&h(t,a,{get:()=>n[a],enumerable:!(r=g(n,a))||r.enumerable});return t};var A=(t,n,i)=>(i=t!=null?k(w(t)):{},u(n||!t||!t.__esModule?h(i,\"default\",{value:t,enumerable:!0}):i,t)),x=t=>u(h({},\"__esModule\",{value:!0}),t);var m=v((I,p)=>{p.exports=_jsx_runtime});var E={};q(E,{default:()=>y,frontmatter:()=>D});var e=A(m()),D={title:\"1.1 ADT operations: stack, queue, and function pointers\",description:\"Study ADT contracts first, then follow stack and queue operations through concrete C implementations and function-pointer-based dispatch.\"};function f(t){let n={code:\"code\",h2:\"h2\",li:\"li\",ol:\"ol\",p:\"p\",pre:\"pre\",ul:\"ul\",...t.components},{CommonMistake:i,Definition:r,InteractiveWidget:a,QuickCheck:c,RevealSolution:s,TheoremCard:d,WorkedExample:o}=n;return i||l(\"CommonMistake\",!0),r||l(\"Definition\",!0),a||l(\"InteractiveWidget\",!0),c||l(\"QuickCheck\",!0),s||l(\"RevealSolution\",!0),d||l(\"TheoremCard\",!0),o||l(\"WorkedExample\",!0),(0,e.jsxs)(e.Fragment,{children:[(0,e.jsx)(n.h2,{children:\"Why this section starts with contracts\"}),`\n`,(0,e.jsx)(n.p,{children:`In CSCI2520, the first mistake is usually not a missing semicolon. It is a\nmissing contract.`}),`\n`,(0,e.jsx)(n.p,{children:`An abstract data type tells us four things before we commit to a particular\nrepresentation:`}),`\n`,(0,e.jsxs)(n.ul,{children:[`\n`,(0,e.jsx)(n.li,{children:\"what operations are legal,\"}),`\n`,(0,e.jsx)(n.li,{children:\"what each operation guarantees,\"}),`\n`,(0,e.jsx)(n.li,{children:\"what the client may assume after a call,\"}),`\n`,(0,e.jsx)(n.li,{children:\"how empty-state and error cases are handled.\"}),`\n`]}),`\n`,(0,e.jsx)(n.p,{children:`That separation matters because the same stack or queue interface can be backed\nby a fixed array, a dynamic array, or a linked list. The client code should not\nneed to change when the representation changes.`}),`\n`,(0,e.jsx)(r,{title:\"ADT contract versus implementation\",children:(0,e.jsx)(n.p,{children:`An ADT contract names the operations and their meaning.\nAn implementation chooses the data fields, memory layout, and update strategy.\nThe contract is the promise; the representation is the mechanism.`})}),`\n`,(0,e.jsx)(n.h2,{children:\"A stack is a disciplined top-only structure\"}),`\n`,(0,e.jsx)(n.p,{children:`The lecture slides define a stack as a pile that can be accessed only from the\ntop. That is not just an analogy. It is the invariant that makes stack code\npredictable.`}),`\n`,(0,e.jsxs)(r,{title:\"Stack core operations\",children:[(0,e.jsxs)(n.p,{children:[\"For a stack \",(0,e.jsx)(n.code,{children:\"S\"}),\":\"]}),(0,e.jsxs)(n.ul,{children:[`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"push(x)\"}),\": place \",(0,e.jsx)(n.code,{children:\"x\"}),\" on the top of \",(0,e.jsx)(n.code,{children:\"S\"}),\",\"]}),`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"pop()\"}),\": remove and return the top element,\"]}),`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"top()\"}),\": inspect the top element without removing it,\"]}),`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"isEmpty()\"}),\": test whether the stack has no elements,\"]}),`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"StackDepth()\"}),\": return the number of stored elements.\"]}),`\n`]})]}),`\n`,(0,e.jsxs)(n.p,{children:[\"The key law is LIFO, last in first out. If \",(0,e.jsx)(n.code,{children:\"a\"}),\" is pushed before \",(0,e.jsx)(n.code,{children:\"b\"}),\", then \",(0,e.jsx)(n.code,{children:\"b\"}),`\nmust come out first unless an error interrupts the process.`]}),`\n`,(0,e.jsxs)(o,{title:\"Tracing a stack by state\",children:[(0,e.jsx)(n.p,{children:\"Start with an empty stack.\"}),(0,e.jsxs)(n.ol,{children:[`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"push(10)\"}),\" gives \",(0,e.jsx)(n.code,{children:\"[10]\"})]}),`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"push(20)\"}),\" gives \",(0,e.jsx)(n.code,{children:\"[10, 20]\"})]}),`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"top()\"}),\" returns \",(0,e.jsx)(n.code,{children:\"20\"}),\" and leaves the state unchanged\"]}),`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"pop()\"}),\" returns \",(0,e.jsx)(n.code,{children:\"20\"}),\" and the stack becomes \",(0,e.jsx)(n.code,{children:\"[10]\"})]}),`\n`]})]}),`\n`,(0,e.jsx)(n.h2,{children:\"The stack interface hides the representation\"}),`\n`,(0,e.jsx)(n.p,{children:\"The lecture makes a point of the opaque type pattern:\"}),`\n`,(0,e.jsx)(n.pre,{children:(0,e.jsx)(n.code,{className:\"language-c\",children:`typedef struct stackCDT *stackADT;\ntypedef int stackElementT;\n\nstackADT EmptyStack(void);\nvoid Push(stackADT stack, stackElementT element);\nstackElementT Pop(stackADT stack);\nint StackDepth(stackADT stack);\nint StackIsEmpty(stackADT stack);\n`})}),`\n`,(0,e.jsxs)(n.p,{children:[\"This is not cosmetic. \",(0,e.jsx)(n.code,{children:\"stackADT\"}),` is a pointer to an incomplete structure, so the\nclient can hold and pass stacks around but cannot reach inside the structure.\nThat means the implementation can change from:`]}),`\n`,(0,e.jsxs)(n.ul,{children:[`\n`,(0,e.jsx)(n.li,{children:\"a fixed array of 100 entries,\"}),`\n`,(0,e.jsxs)(n.li,{children:[\"to a dynamic array with \",(0,e.jsx)(n.code,{children:\"realloc\"}),\",\"]}),`\n`,(0,e.jsx)(n.li,{children:\"to another internal layout later,\"}),`\n`]}),`\n`,(0,e.jsx)(n.p,{children:\"without rewriting client code.\"}),`\n`,(0,e.jsx)(d,{title:\"Representation independence\",children:(0,e.jsx)(n.p,{children:`If the client can use an ADT only through its declared operations, then the\nimplementation may change its internal representation without changing the\nclient-visible behavior.`})}),`\n`,(0,e.jsx)(n.h2,{children:\"How the stack is implemented in practice\"}),`\n`,(0,e.jsx)(n.p,{children:\"The source slides show three important implementation ideas.\"}),`\n`,(0,e.jsxs)(n.p,{children:[`First, a fixed array implementation stores values bottom to top and keeps a\n`,(0,e.jsx)(n.code,{children:\"count\"}),\" field for the current depth:\"]}),`\n`,(0,e.jsx)(n.pre,{children:(0,e.jsx)(n.code,{className:\"language-c\",children:`struct stackCDT {\n   stackElementT elements[100];\n   int count;\n};\n`})}),`\n`,(0,e.jsx)(n.p,{children:`This version is simple, but it has a hard capacity limit. If the depth grows\nbeyond 100, the implementation hits stack overflow at the representation level,\nnot the mathematical stack operation.`}),`\n`,(0,e.jsxs)(n.p,{children:[\"Second, a dynamic-array version stores an expandable pointer and a \",(0,e.jsx)(n.code,{children:\"size\"}),`\nfield:`]}),`\n`,(0,e.jsx)(n.pre,{children:(0,e.jsx)(n.code,{className:\"language-c\",children:`struct stackCDT {\n   stackElementT *elements;\n   int count;\n   int size;\n};\n`})}),`\n`,(0,e.jsxs)(n.p,{children:[\"When the array fills, \",(0,e.jsx)(n.code,{children:\"Push()\"}),\" expands the storage with \",(0,e.jsx)(n.code,{children:\"realloc()\"}),`. That gives\nthe client a larger logical stack without changing the interface.`]}),`\n`,(0,e.jsxs)(n.p,{children:[\"Third, the slides also refine \",(0,e.jsx)(n.code,{children:\"Pop()\"}),` to recover memory more carefully when the\nunderlying array is no longer fully used. That is a reminder that a correct\ninterface is not enough; memory policy still matters.`]}),`\n`,(0,e.jsxs)(o,{title:\"A push/pop implementation sketch\",children:[(0,e.jsx)(n.pre,{children:(0,e.jsx)(n.code,{className:\"language-c\",children:`void Push(stackADT stack, stackElementT element) {\n   if (stack->count == stack->size) {\n      stack->size += 10;\n      stack->elements = realloc(\n         stack->elements,\n         stack->size * sizeof(stackElementT)\n      );\n   }\n   stack->elements[stack->count] = element;\n   stack->count++;\n}\n\nstackElementT Pop(stackADT stack) {\n   stack->count--;\n   return stack->elements[stack->count];\n}\n`})}),(0,e.jsx)(n.p,{children:\"The code is intentionally direct:\"}),(0,e.jsxs)(n.ul,{children:[`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"count\"}),\" tracks the next free slot,\"]}),`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"push\"}),\" stores at that slot and increments the count,\"]}),`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"pop\"}),\" decrements the count and returns the old top value.\"]}),`\n`]})]}),`\n`,(0,e.jsx)(n.h2,{children:\"Queue semantics are different, even if the code looks similar\"}),`\n`,(0,e.jsx)(n.p,{children:`The queue ADT is the other side of the same design problem. The operations are\nsimilar in style, but the invariant is FIFO, first in first out.`}),`\n`,(0,e.jsxs)(r,{title:\"Queue core operations\",children:[(0,e.jsxs)(n.p,{children:[\"For a queue \",(0,e.jsx)(n.code,{children:\"Q\"}),\":\"]}),(0,e.jsxs)(n.ul,{children:[`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"enqueue(x)\"}),\": add \",(0,e.jsx)(n.code,{children:\"x\"}),\" at the tail,\"]}),`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"dequeue()\"}),\": remove and return the head element,\"]}),`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"front()\"}),\": inspect the head element without removing it,\"]}),`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"isEmpty()\"}),\": test whether the queue has no elements,\"]}),`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"QueueLength()\"}),\": return the number of stored elements.\"]}),`\n`]})]}),`\n`,(0,e.jsx)(n.p,{children:\"The lecture notes present the queue header in the same opaque-style pattern:\"}),`\n`,(0,e.jsx)(n.pre,{children:(0,e.jsx)(n.code,{className:\"language-c\",children:`typedef struct queueCDT *queueADT;\ntypedef int queueElementT;\n\nqueueADT EmptyQueue(void);\nvoid Enqueue(queueADT queue, queueElementT element);\nqueueElementT Dequeue(queueADT queue);\nint QueueLength(queueADT queue);\nint QueueIsEmpty(queueADT queue);\n`})}),`\n`,(0,e.jsx)(n.p,{children:`Again, the interface says what the user may do; it does not say how the queue is\nstored.`}),`\n`,(0,e.jsx)(n.h2,{children:\"Queue implementations: array, circular array, linked list\"}),`\n`,(0,e.jsx)(n.p,{children:\"The tutorial slides make the implementation tradeoffs explicit.\"}),`\n`,(0,e.jsxs)(n.ul,{children:[`\n`,(0,e.jsx)(n.li,{children:`A fixed-size array is the simplest first version, but it wastes space when\nelements are dequeued from the front and new values are appended at the tail.`}),`\n`,(0,e.jsxs)(n.li,{children:[\"A circular array fixes that waste by wrapping \",(0,e.jsx)(n.code,{children:\"front\"}),\" and \",(0,e.jsx)(n.code,{children:\"rear\"}),` around with\nthe modulus operator.`]}),`\n`,(0,e.jsxs)(n.li,{children:[\"A linked-list queue stores \",(0,e.jsx)(n.code,{children:\"head\"}),\" and \",(0,e.jsx)(n.code,{children:\"tail\"}),` pointers and grows as long as the\nheap can provide memory.`]}),`\n`]}),`\n`,(0,e.jsx)(n.p,{children:`The linked-list version is especially useful when we want the queue to keep\naccepting work items without shifting existing elements around.`}),`\n`,(0,e.jsx)(n.pre,{children:(0,e.jsx)(n.code,{className:\"language-c\",children:`struct cellT {\n   queueElementT element;\n   struct cellT *next;\n};\n\nstruct queueCDT {\n   struct cellT *head;\n   struct cellT *tail;\n};\n`})}),`\n`,(0,e.jsxs)(n.p,{children:[\"That representation makes \",(0,e.jsx)(n.code,{children:\"enqueue\"}),\" and \",(0,e.jsx)(n.code,{children:\"dequeue\"}),\" conceptually simple:\"]}),`\n`,(0,e.jsxs)(n.ul,{children:[`\n`,(0,e.jsx)(n.li,{children:\"a new node is linked at the tail,\"}),`\n`,(0,e.jsx)(n.li,{children:\"the head node is removed and freed,\"}),`\n`,(0,e.jsxs)(n.li,{children:[(0,e.jsx)(n.code,{children:\"QueueIsEmpty()\"}),\" is just \",(0,e.jsx)(n.code,{children:\"head == NULL\"}),\".\"]}),`\n`]}),`\n`,(0,e.jsxs)(o,{title:\"Why a circular array is better than a plain array\",children:[(0,e.jsxs)(n.p,{children:[\"Suppose a queue alternates \",(0,e.jsx)(n.code,{children:\"enqueue\"}),\" and \",(0,e.jsx)(n.code,{children:\"dequeue\"}),\" many times.\"]}),(0,e.jsxs)(n.ul,{children:[`\n`,(0,e.jsx)(n.li,{children:`In a plain array, values near the front can leave empty space that is not\nreused efficiently.`}),`\n`,(0,e.jsx)(n.li,{children:`In a circular array, the front and rear indices wrap around so the earlier\nslots can be reused.`}),`\n`]}),(0,e.jsxs)(n.p,{children:[\"That is why the tutorial explicitly asks what happens if we \",(0,e.jsx)(n.code,{children:\"enqueue\"}),` and\n`,(0,e.jsx)(n.code,{children:\"dequeue\"}),\" repeatedly for millions of operations.\"]})]}),`\n`,(0,e.jsx)(n.h2,{children:\"Function pointers let C simulate method dispatch and callbacks\"}),`\n`,(0,e.jsx)(n.p,{children:`The lecture on function pointers is not random extra material. It explains how\nC can defer behavior until runtime while still staying type-safe.`}),`\n`,(0,e.jsx)(n.p,{children:\"At a minimum, a function pointer stores the address of code:\"}),`\n`,(0,e.jsx)(n.pre,{children:(0,e.jsx)(n.code,{className:\"language-c\",children:`int (*fp)(int);\n`})}),`\n`,(0,e.jsx)(n.p,{children:`The pointer may point to any function with the matching prototype. That means\nthe type signature is the contract.`}),`\n`,(0,e.jsx)(r,{title:\"Function-pointer rule\",children:(0,e.jsx)(n.p,{children:`A function pointer may point only to functions whose parameter list and return\ntype match the declared function-pointer type.`})}),`\n`,(0,e.jsx)(n.p,{children:`The slides then move to a practical ADT example: extending a hash table with a\ncallback-based traversal operation.`}),`\n`,(0,e.jsx)(n.pre,{children:(0,e.jsx)(n.code,{className:\"language-c\",children:`typedef void (*hashtableFnT)(char *, void *);\n\nvoid ForEachEntryDo(hashtableFnT fp, hashtableADT table);\n`})}),`\n`,(0,e.jsx)(n.p,{children:\"Client code can pass a callback:\"}),`\n`,(0,e.jsx)(n.pre,{children:(0,e.jsx)(n.code,{className:\"language-c\",children:`void DisplayWordCount(hashtableADT table) {\n   ForEachEntryDo(PrintEntry, table);\n}\n`})}),`\n`,(0,e.jsx)(n.p,{children:`This pattern matters for ADTs because it lets the implementation perform the\niteration while the client controls what happens to each entry. The traversal\nlogic stays in one place, but the action can vary.`}),`\n`,(0,e.jsxs)(n.p,{children:[`The same idea also appears in dispatch tables: a set of function pointers can\nselect the correct operation for a specific structure or command without a long\nchain of `,(0,e.jsx)(n.code,{children:\"if\"}),\" statements.\"]}),`\n`,(0,e.jsx)(n.pre,{children:(0,e.jsx)(n.code,{className:\"language-c\",children:`void ApplyOperator(char c, stackADT stack) {\n   int y = Pop(stack);\n   int x = Pop(stack);\n\n   switch (c) {\n   case '+': Push(stack, x + y); break;\n   case '-': Push(stack, x - y); break;\n   case '*': Push(stack, x * y); break;\n   case '/': Push(stack, x / y); break;\n   }\n}\n`})}),`\n`,(0,e.jsx)(n.p,{children:`In this example, the stack stores operands while the operation table decides how\nto combine them. The code is small, but the concept is broad: function pointers\nturn C into a manually controlled dispatch system.`}),`\n`,(0,e.jsx)(a,{id:\"adt-stack-queue-stepper\"}),`\n`,(0,e.jsx)(n.h2,{children:\"Testing an ADT contract with code\"}),`\n`,(0,e.jsx)(n.p,{children:`Once the interface is fixed, the next question is not \\u201Cdoes it compile?\\u201D but\n\\u201Cdoes every observable state transition still satisfy the contract?\\u201D`}),`\n`,(0,e.jsx)(n.p,{children:\"One practical method is to write tiny trace-based tests:\"}),`\n`,(0,e.jsx)(n.pre,{children:(0,e.jsx)(n.code,{className:\"language-c\",children:`stackADT s = EmptyStack();\nPush(s, 10);\nPush(s, 20);\nassert(StackDepth(s) == 2);\nassert(Pop(s) == 20);\nassert(Pop(s) == 10);\nassert(StackIsEmpty(s));\n`})}),`\n`,(0,e.jsx)(n.p,{children:`This kind of test is valuable because it checks the contract at the ADT level,\nnot the memory layout level. If you later replace the array-backed stack with a\nlinked representation, the same test should still pass unchanged.`}),`\n`,(0,e.jsxs)(n.p,{children:[\"The same idea applies to queues: a short \",(0,e.jsx)(n.code,{children:\"enqueue\"}),\", \",(0,e.jsx)(n.code,{children:\"enqueue\"}),\", \",(0,e.jsx)(n.code,{children:\"dequeue\"}),`,\n`,(0,e.jsx)(n.code,{children:\"dequeue\"}),` trace should confirm that the first inserted element is still the\nfirst one removed, even after the representation changes.`]}),`\n`,(0,e.jsx)(n.h2,{children:\"Common mistakes\"}),`\n`,(0,e.jsx)(i,{title:\"Mixing the stack and queue invariants\",children:(0,e.jsxs)(n.p,{children:[(0,e.jsx)(n.code,{children:\"push\"}),\"/\",(0,e.jsx)(n.code,{children:\"pop\"}),\" belong to a stack; \",(0,e.jsx)(n.code,{children:\"enqueue\"}),\"/\",(0,e.jsx)(n.code,{children:\"dequeue\"}),` belong to a queue. Renaming\nthe functions without preserving the access order breaks the ADT contract even\nif the program still compiles.`]})}),`\n`,(0,e.jsx)(i,{title:\"Forgetting error policy on empty structures\",children:(0,e.jsx)(n.p,{children:`The slides explicitly treat empty-stack underflow and queue-empty cases as\nerrors that must be handled. Decide whether the function returns a status code,\na sentinel, or an error indicator before you code the interface.`})}),`\n`,(0,e.jsx)(i,{title:\"Confusing interface stability with implementation stability\",children:(0,e.jsx)(n.p,{children:`The interface should stay stable. The internal representation is what should be\nfree to change when the data structure evolves.`})}),`\n`,(0,e.jsx)(n.h2,{children:\"Quick checks\"}),`\n`,(0,e.jsx)(c,{prompt:\"After `push(1)`, `push(2)`, `pop()`, what remains in the stack?\",children:(0,e.jsx)(n.p,{children:\"Apply LIFO strictly.\"})}),`\n`,(0,e.jsx)(s,{title:\"Answer\",children:(0,e.jsxs)(n.p,{children:[\"Only \",(0,e.jsx)(n.code,{children:\"1\"}),\" remains.\"]})}),`\n`,(0,e.jsx)(c,{prompt:\"After `enqueue(4)`, `enqueue(7)`, `dequeue()`, which element is returned?\",children:(0,e.jsx)(n.p,{children:\"Apply FIFO strictly.\"})}),`\n`,(0,e.jsx)(s,{title:\"Answer\",children:(0,e.jsxs)(n.p,{children:[(0,e.jsx)(n.code,{children:\"4\"}),\" is returned.\"]})}),`\n`,(0,e.jsx)(c,{prompt:\"Why does `ForEachEntryDo(PrintEntry, table)` need a callback type, not just a generic pointer?\",children:(0,e.jsx)(n.p,{children:\"Focus on type checking and matching parameter lists.\"})}),`\n`,(0,e.jsx)(s,{title:\"Guided solution\",children:(0,e.jsx)(n.p,{children:\"The callback type says exactly which function signatures are legal. A generic pointer does not preserve the parameter and return-type information, so the compiler cannot enforce the contract. The callback type keeps the traversal safe and explicit.\"})}),`\n`,(0,e.jsx)(n.h2,{children:\"Exercises\"}),`\n`,(0,e.jsx)(c,{prompt:\"Write a precondition and a postcondition for `Pop(stack)` on a non-empty stack.\",children:(0,e.jsx)(n.p,{children:\"Keep the contract precise.\"})}),`\n`,(0,e.jsx)(s,{title:\"Guided solution\",children:(0,e.jsx)(n.p,{children:\"Precondition: the stack is not empty. Postcondition: the old top element is returned, the stack depth decreases by one, and the order of the remaining elements is unchanged.\"})}),`\n`,(0,e.jsx)(c,{prompt:\"Explain why a queue implemented with a linked list can support `enqueue` without shifting existing elements.\",children:(0,e.jsxs)(n.p,{children:[\"Tie your answer to \",(0,e.jsx)(n.code,{children:\"head\"}),\" and \",(0,e.jsx)(n.code,{children:\"tail\"}),\".\"]})}),`\n`,(0,e.jsx)(s,{title:\"Guided solution\",children:(0,e.jsx)(n.p,{children:\"The new node is linked after the current tail, and the tail pointer is updated to the new node. No earlier node has to move, so the existing queue order stays intact.\"})}),`\n`,(0,e.jsx)(c,{prompt:\"Write one short paragraph explaining how function pointers help testing an ADT implementation.\",children:(0,e.jsx)(n.p,{children:\"Use the interface-versus-implementation distinction.\"})}),`\n`,(0,e.jsx)(s,{title:\"Guided solution\",children:(0,e.jsx)(n.p,{children:\"Function pointers let the same client code call the same signatures while the backend changes. A test suite can therefore exercise multiple implementations of the same ADT contract and catch violations without rewriting the tests.\"})}),`\n`,(0,e.jsx)(n.h2,{children:\"Takeaway\"}),`\n`,(0,e.jsx)(n.p,{children:`The main idea is not \\u201Cstack and queue are two data structures.\\u201D The real idea is\nthat ADT contracts define behavior, implementations choose storage, and function\npointers let C separate those responsibilities without losing control over the\ntypes.`})]})}function y(t={}){let{wrapper:n}=t.components||{};return n?(0,e.jsx)(n,{...t,children:(0,e.jsx)(f,{...t})}):f(t)}function l(t,n){throw new Error(\"Expected \"+(n?\"component\":\"object\")+\" `\"+t+\"` to be defined: you likely forgot to import, pass, or provide it.\")}return x(E);})();\n;return Component;"
  },
  "_id": "textbook/csci2520/adt-and-operations/stack-queue-and-function-operations/en.mdx",
  "_raw": {
    "sourceFilePath": "textbook/csci2520/adt-and-operations/stack-queue-and-function-operations/en.mdx",
    "sourceFileName": "en.mdx",
    "sourceFileDir": "textbook/csci2520/adt-and-operations/stack-queue-and-function-operations",
    "contentType": "mdx",
    "flattenedPath": "textbook/csci2520/adt-and-operations/stack-queue-and-function-operations/en"
  },
  "type": "TextbookUnit",
  "chapterId": "adt-and-operations",
  "course": "csci2520",
  "locale": "en",
  "unitSlug": "stack-queue-and-function-operations",
  "unitId": "csci2520.adt-and-operations.stack-queue-and-function-operations",
  "url": "/en/notes/csci2520/adt-and-operations/stack-queue-and-function-operations"
}