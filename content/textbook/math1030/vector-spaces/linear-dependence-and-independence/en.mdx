---
title: "6.4 Linear dependence and independence"
description: "A theorem-led treatment of redundancy in vector lists, with row-reduction tests and span-preservation consequences."
---

## Motivation

In chapter 6 we started from span: a list of vectors tells us which vectors can
be built by linear combination. The next structural question is whether every
vector in that list is genuinely needed.

If one vector is already generated by the others, then the list has
**redundancy**. A redundant list may still span the same space, but it is not
minimal. Linear dependence and linear independence provide the exact language
for this redundancy question, and they are the bridge to basis and dimension in
later sections.

Practical reasons this section matters:

- in solving `Ax=b`, dependence explains why free variables appear;
- in modeling, dependence identifies repeated features that do not add new
  information;
- in theory, dependence is the criterion that separates arbitrary spanning sets
  from candidate bases.

<InteractiveWidget id="independence-checker" />

## Definitions

<Definition title="Linear independence of a finite list">
Let `V` be a vector space over a field `F`. A list
`(u_1,u_2,\ldots,u_n)` in `V` is **linearly independent** if

$$
\alpha_1u_1+\alpha_2u_2+\cdots+\alpha_nu_n=0
$$

implies `\alpha_1=\alpha_2=\cdots=\alpha_n=0`.
</Definition>

<Definition title="Linear dependence of a finite list">
The same list is **linearly dependent** if there exists a nontrivial choice of
scalars (not all zero) with

$$
\alpha_1u_1+\alpha_2u_2+\cdots+\alpha_nu_n=0.
$$
</Definition>

<Definition title="Trivial relation and nontrivial relation">
The equation with all coefficients zero is the **trivial relation**. Any other
relation is **nontrivial**. Independence means only the trivial relation
exists.
</Definition>

## Theorem/Proposition

<TheoremCard title="Dependence is equivalent to one vector being generated by the others">
For a finite list `(u_1,\ldots,u_n)` with `n\ge 2`, the following are
equivalent:

1. The list is linearly dependent.
2. At least one vector in the list is a linear combination of the remaining
   vectors.

So a dependent list always contains a removable vector that does not change the
span.
</TheoremCard>

<TheoremCard title="Row-reduction test for dependence in `R^m`">
Let `u_1,\ldots,u_n\in R^m` and build

$$
A=[u_1\ u_2\ \cdots\ u_n].
$$

Then `u_1,\ldots,u_n` are linearly independent iff the homogeneous system

$$
Ax=0
$$

has only the zero solution. Equivalently, when reducing `A` to RREF, every
column must be a pivot column.
</TheoremCard>

## Proof sketch or proof idea

<CollapsibleProof title="Proof idea for the redundancy criterion">
`(1) \Rightarrow (2)`: start from a nontrivial relation
`\sum_{i=1}^n\alpha_i u_i=0`. Choose an index `k` with `\alpha_k\ne 0`.
Rearrange:

$$
u_k=-\sum_{i\ne k}\frac{\alpha_i}{\alpha_k}u_i,
$$

so `u_k` is generated by the remaining vectors.

`(2) \Rightarrow (1)`: if
`u_k=\sum_{i\ne k}\beta_i u_i`, move all terms to one side. You obtain a
nontrivial relation, hence dependence.

The key logic: dependence is exactly the existence of at least one solvable
elimination step inside the list.
</CollapsibleProof>

<CollapsibleProof title="Proof idea for the row-reduction criterion">
By definition, dependence asks whether
`\alpha_1u_1+\cdots+\alpha_nu_n=0` has a nonzero coefficient vector.
Writing coefficients as `x=(\alpha_1,\ldots,\alpha_n)^T`, this is exactly
`Ax=0` where columns are the vectors. Elementary row operations preserve the
solution set of a homogeneous system, so the question is unchanged in RREF.
A free variable gives a nonzero solution and therefore dependence; no free
variables give only the zero solution and therefore independence.
</CollapsibleProof>

## Worked examples

<WorkedExample title="A dependent list in `R^3`">
Take

$$
u_1=(1,0,1),\quad u_2=(0,1,1),\quad u_3=(1,1,2).
$$

Because `u_3=u_1+u_2`, we immediately get

$$
u_1+u_2-u_3=0,
$$

which is nontrivial. Hence the list is dependent.
</WorkedExample>

<WorkedExample title="Independence test by RREF">
Let

$$
u_1=(1,2,0),\quad u_2=(0,1,1),\quad u_3=(2,5,1).
$$

Set `A=[u_1\ u_2\ u_3]`:

$$
A=\begin{bmatrix}
1&0&2\\
2&1&5\\
0&1&1
\end{bmatrix}
\xrightarrow{R_2-2R_1}
\begin{bmatrix}
1&0&2\\
0&1&1\\
0&1&1
\end{bmatrix}
\xrightarrow{R_3-R_2}
\begin{bmatrix}
1&0&2\\
0&1&1\\
0&0&0
\end{bmatrix}.
$$

Column 3 is non-pivot, so `x_3` is free in `Ax=0`. Therefore the list is
dependent.
</WorkedExample>

<WorkedExample title="Why a list containing the zero vector is dependent">
Suppose `u_2=0`. Then

$$
0\cdot u_1+1\cdot u_2+0\cdot u_3+\cdots+0\cdot u_n=0
$$

is already a nontrivial relation (coefficient of `u_2` is `1`). So any list
containing `0` is dependent.
</WorkedExample>

## Common mistakes

<CommonMistake title="Confusing dependence with not spanning">
Dependent lists can still span a large space. Dependence only says the list is
not minimal.
</CommonMistake>

<CommonMistake title="Testing `Au=b` instead of `Ax=0`">
Independence is always tested with the homogeneous equation. Solving a
non-homogeneous system answers a different question.
</CommonMistake>

<CommonMistake title="Forgetting that order does not matter for dependence">
Permuting vectors changes their positions but not whether a nontrivial relation
exists.
</CommonMistake>

## Summary

- Independence means uniqueness of the zero-coefficient representation of `0`.
- Dependence means at least one vector is generated by the others.
- For column vectors, row reduction gives an algorithmic criterion through
  pivots and free variables.
- This section is the structural prerequisite for basis and dimension.

## Exercises

<QuickCheck prompt="Decide whether `(1,0,0),(0,1,0),(1,1,0)` in `R^3` is independent.">
Use either the redundancy criterion or the RREF test.
</QuickCheck>

<QuickCheck prompt="Let `S=\{(1,1),(2,2)\}` in `R^2`. Is `S` independent?">
Try to write one vector as a scalar multiple of the other.
</QuickCheck>

<QuickCheck prompt="If `\{u_1,u_2,u_3\}` is independent, can `\{u_1,u_2,u_3,u_1+u_2\}` be independent?">
Translate the question into a linear relation.
</QuickCheck>

## Solutions

<RevealSolution title="Exercise 1">
Dependent, because `(1,1,0)=(1,0,0)+(0,1,0)`.
</RevealSolution>

<RevealSolution title="Exercise 2">
Dependent, because `(2,2)=2(1,1)`.
</RevealSolution>

<RevealSolution title="Exercise 3">
No. `u_1+u_2-(u_1+u_2)=0` is a nontrivial relation in the larger list.
</RevealSolution>

## Read this first

This section builds directly on
[6.3 Linear combinations and span](/en/notes/math1030/vector-spaces/linear-combinations-and-span)
and the row-reduction workflow in
[2.3 Gaussian elimination and RREF](/en/notes/math1030/matrices/gaussian-elimination-rref).
